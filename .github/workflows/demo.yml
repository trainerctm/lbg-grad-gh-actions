name: Simple CI Workflow

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run basic setup commands
        run: |
          echo "Running a simple CI workflow"
          echo "This can run anywhere and does not require specific language setup"
          echo "Listing files in the repository:"
          ls -al

      - name: Run tests
        run: |
          echo "Running example tests"
          # Example test command, replace with actual tests if needed
          echo "Test passed!"

      - name: Cleanup
        run: |
          echo "Cleanup steps can go here"
          echo "Workflow complete!"
